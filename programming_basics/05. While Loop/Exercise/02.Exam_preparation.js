function exam(input){

let badGrades = Number(input[0]);
let exc = input[index];
let evaluation = input[index + 1];
let totalSum = 0;
let average = 0;

let index = 1;
while(badGrades < input[index]){
    exc = input[index];
    evaluation = input[index + 1];
    totalSum += evaluation;
    average = totalSum / 

    if(input[index] === "Enough"){
        break;
    }
}

}
exam